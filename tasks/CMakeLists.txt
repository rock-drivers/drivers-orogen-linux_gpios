# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(linux_gpiosTaskLib)
ADD_LIBRARY(${LINUX_GPIOS_TASKLIB_NAME} SHARED 
    ${LINUX_GPIOS_TASKLIB_SOURCES})
add_dependencies(${LINUX_GPIOS_TASKLIB_NAME}
    regen)



TARGET_LINK_LIBRARIES(${LINUX_GPIOS_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${LINUX_GPIOS_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${LINUX_GPIOS_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${LINUX_GPIOS_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${LINUX_GPIOS_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${LINUX_GPIOS_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${LINUX_GPIOS_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${LINUX_GPIOS_TASKLIB_HEADERS}
    DESTINATION include/orocos/linux_gpios)

